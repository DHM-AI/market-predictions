# Dashboard-only requirements — used by Streamlit Community Cloud.
# Slim list (no xgboost / sklearn / anthropic) so Streamlit redeploys
# in ~30-60s instead of 2-3 minutes.
#
# For agent / model / analyst code (GitHub Actions), use
# requirements-agent.txt which includes everything below PLUS the
# heavy ML & Claude packages.

streamlit>=1.35
pandas>=2.2
numpy>=1.26
plotly>=5.20
ta>=0.11
yfinance>=0.2.40
alpaca-py>=0.26
supabase>=2.4
python-dotenv>=1.0
requests>=2.31
lxml>=5.0
feedparser>=6.0
